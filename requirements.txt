# ── Runtime dependencies (installed in the Cloud Run image) ───────────────────
# Pins reflect the versions verified to work together in this project.

# Core dashboard framework
vizro==0.1.56
vizro-dash-components==0.1.1
dash==4.1.0
dash-bootstrap-components==2.0.4
dash_ag_grid==33.3.3
dash_mantine_components==2.7.0
plotly==6.7.0

# Web server / sessions
Flask==3.1.3
Flask-Caching==2.4.0
Flask-Compress==1.18
Werkzeug==3.1.8
gunicorn==23.0.0
# Only used if CACHE_REDIS_URL is set (Flask-Caching's RedisCache backend);
# unused/no-op otherwise.
redis==5.2.1
# Already a transitive dep of the google-cloud libs; pinned directly since
# charts_discover.py now calls OpenAI's embeddings endpoint with it.
requests==2.34.2

# Data
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1

# Config
pydantic==2.13.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# ── GCP / Auth ────────────────────────────────────────────────────────────────
# Imported lazily, so local dev (AUTH_ENABLED=false, CSV fixtures) works even
# before these are installed. Required only when AUTH_ENABLED=true or when a
# dashboard actually queries BigQuery.
firebase-admin==6.5.0
google-cloud-firestore==2.19.0
google-cloud-bigquery==3.27.0
google-cloud-bigquery-storage==2.27.0
db-dtypes==1.3.1
pyarrow==18.1.0
